# DROP - s6a: tree convergence

2 files. Your uploaded repo showed select.html + experiments.html are pre-s4a
(that delta never fully landed). These are the current tested versions:
smart-localhost back/brand links on select, plate id + smart script on the
floor. No behavior change on the served floor (the server-side rewrite already
covers it) - this just makes disk match the tested tree.
