Trained weights live here at runtime and are NEVER committed
(same discipline as datasets in INFO_698_experiments). Copy promoted
checkpoints in by hand or via fetch script; record provenance beside them.
