# genre/app — mini-stack (copied from /_app_template)

Reads sibling `../data/<phase>/eda_stats.json` and `../eda/figures/<phase>/`.
```bash
pip install flask
python projects/genre/app/server.py     # -> http://127.0.0.1:5000
```
